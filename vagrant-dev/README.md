# Development machine image

(Vagrant)[https://www.vagrantup.com/] `Vagrantfile` to create development
server for working on this. Roughly the same as a possible production 
server.

Personal preference: ubuntu-flavoured.

```
vagrant up
vagrant ssh
```

